services:
  - type: web
    name: hamada-live-web
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: EULER_API_KEY
        sync: false
